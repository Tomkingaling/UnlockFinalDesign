A compact mid-page email capture band (lighter than the full waiting-list form).

```jsx
<LeadCaptureInline onSubmit={(email) => subscribe(email)} />
<LeadCaptureInline heading="Get early access" cta="Notify me" note="We'll email you at launch." onSubmit={…} />
```
